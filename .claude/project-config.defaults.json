{
  "_comment": "Shipped defaults for apexyard project-level config. Do NOT edit this file directly in a fork — override specific keys in .claude/project-config.json instead. Upstream changes to defaults propagate via /update.",
  "_schema_version": 1,

  "mail_design": {
    "_comment": "Config for the mail-design linter (.claude/hooks/mail-lint.py), the /mail-review skill and Barid (.claude/agents/mail-reviewer.md). TWO TIERS, deliberately. The 'universal' rules are true of HTML email everywhere and ship ON by default, so a fresh adopter gets value with zero configuration. Everything brand-specific ships EMPTY, and the checks that depend on it SKIP LOUDLY rather than inventing a house style. Fill those in from your own mail spec and point spec_path at it. SHALLOW MERGE: defining mail_design in .claude/project-config.json REPLACES this whole subtree, so copy across every key you want to keep — dropping template_globs by accident is a hard error, not a silent pass.",
    "spec_path": "",
    "template_globs": ["emails/**/*.html", "**/email-templates/**/*.html"],
    "universal": {
      "_comment": "True of HTML email regardless of brand. Setting one to false is recorded as a SKIP, never as a pass.",
      "forbid_alpha_colour": true,
      "forbid_layout_style_block": true,
      "forbid_third_party_fonts": true,
      "forbid_script": true,
      "forbid_css_filter": true,
      "require_img_alt": true,
      "require_img_width": true,
      "require_table_layout": true,
      "forbid_modern_layout": true,
      "expected_width_px": 600
    },
    "palette": {},
    "allow_hex_outside_palette": [],
    "fonts": {
      "_comment": "Each entry names a face that may only appear when one of its fallbacks appears in the SAME font-family declaration. This is the rule that makes a webfont progressive enhancement rather than a gamble.",
      "stacks": []
    },
    "families": {
      "_comment": "Named families, each with a signature_ground hex. The family is detected from that hex appearing as a band ground, or forced with --family. Per-family keys: signature_ground (hex, required), max_signature_bands (int, default 1), requires_photo (bool - satisfied by an image at least photo_min_width px wide, NOT by the header logo), photo_min_width (int, default 400), allows_unsubscribe (bool - false makes an unsubscribe link an error, which is what keeps a clinical mail off the marketing consent gate)."
    },
    "cta": {
      "_comment": "fill is the action colour. Its contrast against the ground it sits on is ALWAYS measured against contrast.non_text_min; allowed_grounds is an additional, optional allowlist.",
      "fill": "",
      "allowed_grounds": [],
      "max_per_email": 1
    },
    "contrast": {
      "text_min": 4.5,
      "non_text_min": 3.0,
      "accepted_exceptions": []
    },
    "copy": {
      "_comment": "forbidden_patterns are regexes run against the template; forbidden_words are whole-word and case-insensitive. Both are compiled defensively and run under a timeout — a bad pattern is reported as a config defect, never a crash or a hang.",
      "forbidden_patterns": [],
      "forbidden_words": [],
      "placeholder_tokens": ["PHOTO_URL", "CTA_URL", "LOGO_URL"],
      "variable_syntax": "\\{\\{\\s*(\\w+)\\s*\\}\\}"
    }
  },
  "ticket": {
    "prefix_whitelist": [
      "Feature",
      "Bug",
      "Chore",
      "Refactor",
      "Testing",
      "CI",
      "Docs",
      "Spike",
      "Prototype",
      "Investigation",
      "Migration",
      "Idea"
    ],
    "label_priority_scheme": "P0,P1,P2,P3",
    "required_sections": {
      "Feature": ["User Story", "Acceptance Criteria"],
      "Chore": ["Driver", "Scope", "Acceptance Criteria"],
      "Refactor": ["Driver", "Scope", "Acceptance Criteria"],
      "Testing": ["Driver", "Scope", "Acceptance Criteria"],
      "CI": ["Driver", "Scope", "Acceptance Criteria"],
      "Docs": ["Driver", "Acceptance Criteria"],
      "Bug": ["Given / When / Then", "Repro"],
      "Spike": ["Hypothesis", "Budget", "Kill Criteria", "Disposition"],
      "Prototype": ["Direction Question", "Fidelity", "Budget", "Disposition"],
      "Investigation": ["Trigger", "Hypothesis being tested", "Method", "Findings", "Conclusion", "Follow-up actions"]
    },
    "skip_marker": "<!-- validate-issue-structure: skip -->",
    "_schema_exempt_skills_comment": "Skills that file [Feature]/[Bug] issues UPSTREAM (me2resh/apexyard) using their own body template, which is deliberately distinct from this project's required_sections schema. When one of these is the active filing skill (marker at .claude/session/active-issue-skill), validate-issue-structure.sh skips the project schema for that create — they are the trusted producers of those bodies. Narrow by design: a project /feature or /bug writes its own name to the marker and is NOT exempted. See me2resh/apexyard#712.",
    "schema_exempt_skills": [
      "request-apexyard-feature",
      "report-apexyard-bug"
    ],
    "_bootstrap_skills_comment": "Skills that run BEFORE any ticket can exist (fork bootstrap, project handover, framework upstream-sync). When one of these is active (marker at .claude/session/active-bootstrap), require-active-ticket.sh exempts the gate. The marker is written by the skill itself on entry, cleared on completion, AND reset at SessionStart so a stale marker can't carry over. See AgDR-0011-bootstrap-skill-exemption.md and me2resh/apexyard#150.",
    "bootstrap_skills": [
      "setup",
      "handover",
      "update",
      "split-portfolio",
      "onboard",
      "tutorial"
    ],
    "_create_command_patterns_comment": "Raw ticket-create CLI shapes that require-skill-for-issue-create.sh blocks unless a structured ticket skill (/task, /feature, /bug, /spike, /migration, /investigation, /idea) is in flight (marker at .claude/session/active-issue-skill). Tracker-agnostic: extend this list in .claude/project-config.json for Linear, Jira, Asana, or custom trackers. Patterns are matched as substrings of the (whitespace-collapsed) bash command. See AgDR-0030 and me2resh/apexyard#268.",
    "create_command_patterns": [
      "gh issue create",
      "gh api repos/",
      "glab issue create",
      "linear issue create",
      "jira issue create",
      "jira create",
      "asana task create",
      "tracker_create"
    ]
  },

  "branch": {
    "type_whitelist": [
      "feature",
      "fix",
      "refactor",
      "chore",
      "docs",
      "test",
      "spike",
      "prototype",
      "ci",
      "build",
      "perf",
      "sync"
    ],
    "_trunk_whitelist_comment": "Trunk/shared-integration branch names validate-branch-name.sh exempts from the {type}/{TICKET}-{description} shape entirely. Includes `development` — the gitflow long form some Bitbucket/gitflow remotes use as the integration branch, missed by the original hardcoded main/master/develop/dev list. Extend in .claude/project-config.json (REPLACES this array) to add client-mandated trunk names (e.g. `trunk`, `staging`). See me2resh/apexyard#888.",
    "trunk_whitelist": [
      "main",
      "master",
      "develop",
      "dev",
      "development"
    ]
  },

  "commit": {
    "type_whitelist": [
      "feat",
      "fix",
      "refactor",
      "test",
      "docs",
      "chore",
      "style",
      "perf",
      "build",
      "ci",
      "revert",
      "spike",
      "prototype",
      "sync"
    ]
  },

  "pr": {
    "title_type_whitelist": [
      "feat",
      "fix",
      "docs",
      "style",
      "refactor",
      "perf",
      "test",
      "build",
      "ci",
      "chore",
      "revert",
      "release",
      "spike",
      "prototype",
      "sync"
    ],
    "required_sections": [
      "Testing",
      "Glossary"
    ],
    "skip_marker": "<!-- pr-sections: skip -->",
    "allow_multiple_closes": false,
    "multi_close_skip_marker": "<!-- multi-close: approved -->"
  },

  "mcp_search": {
    "_comment": "Config for suggest-mcp-search.sh. gate_mode is OPT-IN (default false): when true AND the apexyard-search MCP server is configured, the hook SOFT-BLOCKS (exit 2) exploratory `grep -r`/`find` over indexed framework/project paths, instructing the agent to use search_code/search_docs first. Read/Glob/Grep stay advisory-only (never blocked — read→edit needs real line numbers). Per-call escape hatch: prefix the command with APEXYARD_MCP_FALLBACK=1 (or export it) to proceed when MCP genuinely can't serve the query (empty index, non-indexable repo, stale index). Default false keeps the hook advisory-only for everyone who doesn't opt in. See AgDR-0070, #651.",
    "gate_mode": false
  },

  "leak_protection": {
    "public_framework_repos": [
      "me2resh/apexyard"
    ],
    "auto_detect_upstream": true,
    "skip_marker": "<!-- private-refs: allow -->"
  },

  "review_markers": {
    "on_stale": "warn",
    "human_approver_title": "CEO",
    "require_posted_review": false,
    "_comment_require_posted_review": "me2resh/apexyard#1051 — OPT-IN. When true, block-unreviewed-merge.sh additionally requires that a code review was actually POSTED to the PR at the same commit the Rex marker names, verified via the forge API. Default false because it changes merge behaviour for every adopter and is GitHub-only today (glab/custom/none skip with a warning; see tracker_review_at_sha in _lib-tracker.sh). Turn it on when you want 'Rex reviewed this' to be verifiable server-side instead of inferred from a local file an agent can write. Deliberately WEAKER than the independent-reviewer gate AgDR-0062 deferred: it accepts a review posted by the PR's own author, so it is NOT separation of duties and must not be described as such. What it does prove is that a review exists on the forge at this commit, which a local file write cannot fabricate. See AgDR-0112.",
    "_comment_human_approver_title": "me2resh/apexyard#957 — DISPLAY-ONLY override for the prose word used for the human per-PR merge approver in hook messages and /approve-merge output (e.g. 'Maintainer', 'Dev Lead'). Default 'CEO' is a zero-behaviour-change no-op. Marker filenames stay '-ceo.approved', and the structured fields (sha=, approved_by=user, skill_version=) are UNCHANGED regardless of this value — this key only affects printed text, never gate logic. See docs/agdr/AgDR-0103-configurable-approver-display-title.md."
  },

  "docs_index": {
    "_comment": "me2resh/apexyard#753 — the maintain-docs-index.sh PostToolUse hook. OFF by default: it writes a tracked docs/INDEX.md into projects/<name>/docs/ on doc edits, so adopters opt in. When enabled it (1) regenerates the per-project docs/INDEX.md (grouped, git-dated TOC) and (2) nudges when a *.md/*.html lands under projects/<name>/ outside docs/. Advisory only — never blocks.",
    "enabled": false,
    "index_filename": "INDEX.md"
  },

  "git": {
    "protected_branches": [
      "main",
      "master",
      "dev",
      "develop"
    ]
  },

  "pre_push": {
    "commands": [],
    "_comment": "Default is empty — per-fork overrides (see docs/project-config.md) populate this with the commands the project actually runs (lint, typecheck, test, build). An empty list makes pre-push-gate.sh a no-op."
  },

  "agdr_trigger_paths": [
    "**/domain/**",
    "**/infrastructure/**",
    "**/migrations/**",
    "infrastructure/**",
    "template.yaml",
    "**/*.tf",
    "**/*.tfvars",
    ".github/workflows/**"
  ],

  "agdr_trigger_dep_files": [
    "package.json",
    "pyproject.toml",
    "Cargo.toml",
    "go.mod",
    "Gemfile"
  ],

  "portfolio": {
    "_comment": "Path resolution for the portfolio registry + per-project docs + ideas backlog + onboarding config + workspace dir + private custom skills/handbooks. Defaults match single-fork mode (paths inside the fork). Override in .claude/project-config.json for split-portfolio mode (e.g. \"../portfolio/apexyard.projects.yaml\"). Relative paths resolve against the ops-fork root (the dir containing the .apexyard-fork marker, or the legacy onboarding.yaml + apexyard.projects.yaml pair). See docs/multi-project.md and AgDR-0010-portfolio-config-and-self-healing.md. The `onboarding` and `workspace_dir` keys were added in framework #242 (split-portfolio v2). The `custom_skills_dir` and `custom_handbooks_dir` keys were added in framework #243 — see docs/multi-project.md § 'Private custom skills + handbooks'.",
    "registry": "./apexyard.projects.yaml",
    "projects_dir": "./projects",
    "ideas_backlog": "./projects/ideas-backlog.md",
    "onboarding": "./onboarding.yaml",
    "workspace_dir": "./workspace",
    "custom_skills_dir": "./custom-skills",
    "custom_handbooks_dir": "./custom-handbooks"
  },

  "tracker": {
    "_comment": "Tracker dispatch — selects which CLI verifies that ticket-IDs reference real tickets. Default is GH (GitHub Issues via `gh issue view`), matching today's behaviour exactly. Supported `kind` values: gh, glab, linear, jira, asana, custom, none. `view_command` is a template with `{id}` and `{owner_repo}` placeholders — the lib substitutes them and runs the result. It is intentionally NOT pinned here: the lib picks a KIND-AWARE built-in default (gh → `gh issue view … --json state,title,url,labels,body`; glab → `glab issue view … --output json`), so a glab/gh adopter who only sets `kind` gets the matching command without also having to set `view_command`. Pinning a default here would deep-merge over a `kind: glab` override and force the gh command (me2resh/apexyard#755). linear/jira/asana/custom adopters still set their own `view_command`. `id_pattern` is the regex `validate-branch-name.sh` and `validate-pr-create.sh` use to shape-check ticket IDs (covers GH `#123` / `GH-123` plus enterprise prefixes like `LIN-456` / `JIRA-789` by default). Set `kind = none` to disable existence verification entirely (shape-only). See AgDR-0033 and `.claude/hooks/_lib-tracker.sh`.",
    "kind": "gh",
    "id_pattern": "^(#[0-9]+|GH-[0-9]+|[A-Z]{2,10}-[0-9]+)$"
  },

  "pdf": {
    "_comment": "Config for the /pdf skill. preferred_converter picks between pandoc / md-to-pdf / wkhtmltopdf when multiple are installed (null = first available in the dispatch order). pdf_engine is passed to pandoc as --pdf-engine; common values are xelatex (best Unicode), pdflatex (smaller install), wkhtmltopdf (no LaTeX needed but degraded typography). default_destination is used when /pdf is invoked with --no-prompt — one of 'workspace' (project repo docs/), 'projects' (ops fork projects/<name>/pdfs/), 'keep' (next to source), or 'ask' (require the interactive prompt; passing --no-prompt with this value is an error). See AgDR-0034.",
    "preferred_converter": "pandoc",
    "pdf_engine": "xelatex",
    "default_destination": "ask"
  },

  "mutation": {
    "_comment": "Config for the /mutation-test skill. runner maps a detected language to its mutation-test tool; threshold is the mutation-score % below which /launch-check WARNs (not FAILs — mutation is a leading indicator, not a launch blocker). timeout_minutes caps a single run so a stuck runner can't hold up the whole milestone-boundary audit. See AgDR-0045.",
    "runner": {
      "ts": "stryker",
      "js": "stryker",
      "python": "mutpy",
      "go": "go-mutesting",
      "ruby": "mutant"
    },
    "threshold": 60,
    "timeout_minutes": 90
  },

  "eval_agents": {
    "_comment": "Config for the /eval-agents skill. corpus_dir is where docs/eval-agents/corpus/<agent>.json files live. thresholds gate the PASS/WARN verdict (non-blocking — this skill is manual/periodic measurement, not a merge gate). approve_precision_min is the headline metric per AgDR-0089 (never the blended concordance spike #825 showed hides real failures). blocking_catch_rate_min=1.0 means ANY missed BLOCKING/HIGH ground-truth defect forces WARN regardless of the aggregate score. See AgDR-0089.",
    "corpus_dir": "docs/eval-agents/corpus",
    "reports_dir": "docs/eval-agents/reports",
    "thresholds": {
      "approve_precision_min": 0.85,
      "blocking_catch_rate_min": 1.0,
      "overall_catch_rate_min": 0.7,
      "false_positive_rate_max": 0.3
    }
  },

  "github_projects": {
    "_comment": "GitHub Projects (v2) board integration. Set enable_auto_moves=true to auto-move cards on SDLC lifecycle events (start-ticket → In progress, PR created → In review, merge → Measurement). board_number and owner must identify your project board. status_map keys map to the option labels on your board's Status single-select field — adjust them to match your board's column names. Override this whole block in .claude/project-config.json. See .claude/hooks/_lib-project-board.sh and me2resh/apexyard#725.",
    "owner": "",
    "board_number": 0,
    "enable_auto_moves": false,
    "status_field_name": "Status",
    "status_map": {
      "in_progress": "In progress",
      "review": "In review",
      "measurement": "Measurement"
    }
  },

  "skill_intent": {
    "_comment": "Config for detect-skill-intent.sh — the skill-side sibling of detect-role-trigger.sh (me2resh/apexyard#894). Each entry maps a set of intent phrases to the skill that owns that work, plus the template it produces (empty string when the skill has no single template, e.g. /launch-check's 10-dimension sweep). Phrases are matched as literal, lowercased substrings against the (punctuation-stripped) prompt — data-driven so adopters can extend the map in .claude/project-config.json (shallow-merge REPLACES this whole array; copy entries you want to keep) without touching hook logic. Advisory only — see .claude/rules/skill-first.md.",
    "map": [
      {
        "skill": "threat-model",
        "template": "templates/audits/threat-model.md",
        "phrases": ["threat model", "threat modeling", "threat modelling", "stride analysis"]
      },
      {
        "skill": "dfd",
        "template": "templates/architecture/dfd.md",
        "phrases": ["data flow diagram", "make a dfd", "draw a dfd", "trust boundary diagram"]
      },
      {
        "skill": "accessibility-audit",
        "template": "templates/audits/accessibility-audit.md",
        "phrases": ["accessibility audit", "a11y audit", "wcag audit", "wcag check"]
      },
      {
        "skill": "compliance-check",
        "template": "templates/audits/compliance-check.md",
        "phrases": ["compliance check", "gdpr audit", "gdpr check", "eprivacy audit"]
      },
      {
        "skill": "seo-audit",
        "template": "templates/audits/seo-audit.md",
        "phrases": ["seo audit", "seo check"]
      },
      {
        "skill": "geo-audit",
        "template": "templates/audits/geo-audit.md",
        "phrases": ["geo audit", "aeo audit", "llms.txt audit"]
      },
      {
        "skill": "performance-audit",
        "template": "templates/audits/performance-audit.md",
        "phrases": ["performance audit", "perf audit", "core web vitals audit"]
      },
      {
        "skill": "monitoring-audit",
        "template": "templates/audits/monitoring-audit.md",
        "phrases": ["monitoring audit", "observability audit"]
      },
      {
        "skill": "docs-audit",
        "template": "templates/audits/docs-audit.md",
        "phrases": ["docs audit", "documentation audit"]
      },
      {
        "skill": "analytics-audit",
        "template": "templates/audits/analytics-audit.md",
        "phrases": ["analytics audit", "event taxonomy audit"]
      },
      {
        "skill": "security-review",
        "template": "templates/audits/security-review.md",
        "phrases": ["security review", "security audit", "owasp review"]
      },
      {
        "skill": "launch-check",
        "template": "",
        "phrases": ["launch check", "production readiness", "go no go review", "launch readiness audit"]
      },
      {
        "skill": "write-spec",
        "template": "templates/prd.md",
        "phrases": ["write a spec", "write a prd", "draft a prd", "draft a spec", "write a feature spec"]
      },
      {
        "skill": "bug",
        "template": "templates/tickets/bug.md",
        "phrases": ["file a bug", "report a bug", "file a bug ticket"]
      },
      {
        "skill": "feature",
        "template": "templates/tickets/feature.md",
        "phrases": ["file a feature", "create a feature ticket", "file a feature ticket"]
      },
      {
        "skill": "plan-initiative",
        "template": "templates/initiative.md",
        "phrases": ["plan this initiative", "plan the quarter", "plan this quarter", "plan our roadmap"]
      },
      {
        "skill": "decide",
        "template": "templates/agdr.md",
        "phrases": ["decide between", "let's decide", "lets decide", "make a technical decision"]
      }
    ]
  }
}
